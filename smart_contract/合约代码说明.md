# Attest Arena — 智能合约代码说明

> 项目定位：Creditcoin EVM 上的「阵营对抗 + 积分链上存证 + 奖池自动分配」。
> Solidity 版本：`^0.8.20`。整套合约分三层：**身份层 → 积分/存证层 → 奖池层**，再加三个**接口**做解耦。

---

## 一、整体架构与依赖关系

```
FactionRegistry（身份底座）
      ▲                    ▲
      │ (IFactionRegistry) │ (IScoreAttester)
      │                    │
ScoreAttester（积分账本 + 存证）──► IAttestation（存证抽象）
      │                                 │
      │ (IScoreAttester)                 ├── SelfAttestation（MVP 自建 hash 存证）
      ▼                                 └── CreditcoinAttestation（真实跨链存证，待核对）
PrizePool（奖池分配）
```

**部署顺序**（Remix）：
1. `SelfAttestation` → 得地址 `AT`
2. `FactionRegistry` → 得地址 `FR`
3. `ScoreAttester(FR, AT)` → 得地址 `SA`
4. `PrizePool(FR, SA)` → 得地址 `PP`
5. 在 `ScoreAttester` 调 `setBackend(后端地址, true)` 授权后端加分

---

## 二、接口文件（3 个，用于跨合约解耦）

### 1. IFactionRegistry.sol
供 `ScoreAttester` / `PrizePool` 跨合约读取身份层数据的最小接口。
- 用 `bool` 查询代替 `enum`，避免跨文件 enum 类型不一致导致编译问题。
- 暴露 7 个只读方法：`isActive` / `isFinished` / `teamExists` / `getTeamFaction` / `getTeamLeader` / `teamCount` / `factionCount`。

### 2. IScoreAttester.sol
供 `PrizePool` 跨合约读取积分账本的最小接口。
- 只有两个方法：`getTeamScore(teamId)` 和 `getFactionScore(factionId)`。

### 3. IAttestation.sol
存证抽象接口，`ScoreAttester` 只依赖它，不关心底层用哪种存证实现。
- 唯一方法：`attest(bytes data) → bytes32 attestationId`。
- 好处：可以在「自建 hash 存证」与「Creditcoin 跨链存证」之间无缝切换，核心逻辑不用改。

---

## 三、核心合约详解

### 1. FactionRegistry.sol（身份底座 + 状态机）

**作用**：管理阵营、团队、玩家地址映射，以及比赛生命周期状态机。**不存积分**（积分只存在 ScoreAttester，避免双份数据不一致）。

**关键数据结构**：
| 结构/变量 | 说明 |
|---|---|
| `GamePhase` 枚举 | `NOT_STARTED / ACTIVE / FINISHED` 三态状态机 |
| `Faction` | `{ id, name }` 阵营 |
| `Team` | `{ id, name, factionId, leader, members[] }` 团队（members 含 leader） |
| `playerTeam` | `address → teamId` 映射，0 表示未加入（防女巫：一人一队） |
| `factionCount` / `teamCount` | 自增 ID 计数器 |
| `owner` | 部署者，拥有管理权限 |

**函数**：
- `createFaction(name)` — 仅 owner，且**只能在 NOT_STARTED** 阶段建阵营。
- `createTeam(name, factionId)` — 任何玩家建队并自动加入成为 leader；校验名字非空、阵营存在、玩家未入队。
- `joinTeam(teamId)` — 任何玩家加入已有团队；同样一人只能一队。
- `startGame()` / `endGame()` — 仅 owner 切换状态机 `NOT_STARTED → ACTIVE → FINISHED`。
- 只读查询：`isActive` / `isFinished` / `teamExists` / `getTeamFaction` / `getTeamLeader` / `getTeamMemberCount`。

---

### 2. ScoreAttester.sol（积分权威账本 + 存证，核心合约）

**作用**：积分唯一权威源。只有授权后端能加减分，**每次变动都生成一条链上存证记录**。

**关键数据结构**：
| 结构/变量 | 说明 |
|---|---|
| `ScoreRecord` | `{ teamId, taskId, scoreChange(int256), timestamp, attestationId }` 单条积分记录 |
| `teamScores` | `teamId → 总分` |
| `factionScores` | `factionId → 总分`（随团队分同步增减） |
| `teamRecords` | `teamId → ScoreRecord[]` 每条历史记录 |
| `isBackend` | `address → bool` 授权后端白名单 |
| `registry` / `attestation` | immutable 依赖注入（身份层 + 存证层） |

**函数**：
- `setBackend(backend, authorized)` — 仅 owner，把后端钱包加入/移出授权名单。
- `addScore(teamId, taskId, scoreChange)` — **核心方法**，仅后端/owner 可调：
  1. 校验比赛在 `ACTIVE` 阶段、团队存在；
  2. 更新团队分和阵营分（支持加分和扣分，扣分有下溢保护）；
  3. 把 `(teamId, factionId, taskId, scoreChange, timestamp)` 编码后调用 `attestation.attest()` 生成存证 ID；
  4. 写入 `teamRecords` 历史，发出 `ScoreAdded` 事件。
- 只读：`getTeamScore` / `getFactionScore` / `getTeamRecordCount` / `getTeamRecord`。

**设计要点**：
- 比赛结束后（FINISHED）`addScore` 会因 `isActive()` 为 false 而 revert，实现**积分锁定**。
- `attestation` 是接口类型，可注入不同实现。

---

### 3. PrizePool.sol（native 币奖池 + 自动分配）

**作用**：托管 native 币，比赛结束后按「获胜阵营 + 阵营内排名」自动分配。团队奖金统一打给 leader 代领。

**分配规则（写死）**：
- 获胜阵营独得**全部**奖池（失败阵营无奖励，增强对抗性）。
- 获胜阵营内：第 1 名 40% / 第 2 名 30% / 第 3 名 20% / 其余团队按积分占比分剩余 10%。
- 若名次有空缺（团队不足 3 个），空缺份额并入第 1 名。

**关键变量**：`totalPrizePool`（累计充值）、`settled`（是否已结算）、`rewards`（`leader地址 → 可领金额`）。

**函数**：
- `receive()` — 允许直接向合约转账（不记录到 totalPrizePool，仅 `depositPrize` 会记录）。
- `depositPrize()` — 仅 owner，结算前随时充值，累加到 `totalPrizePool`。
- `settlePrizes()` — 仅 owner，**只能执行一次**：校验已 FINISHED 且奖池非空 → 找获胜阵营 → 找前 3 团队 → 按比例分配 → 置 `settled=true`。
- `claimReward(teamId)` — 团队 leader 用对应地址领取；查 leader、校验有余额、清零后用 `call` 转账（防重入）。
- 内部方法：
  - `_findWinningFaction()` — 遍历所有阵营，返回积分最高者。
  - `_findTopTeams(factionId)` — 在获胜阵营内用「插入排序」选出积分前 3 的团队。
  - `_allocate(factionId, topTeams)` — 按 40/30/20/10 比例切分并记入 `rewards`。
  - `_credit(teamId, amount)` — 把金额记到该队 leader 名下。

---

### 4. SelfAttestation.sol（MVP 自建 hash 存证）

**作用**：最简单的存证实现，能立即跑通。对 `data` 做 `keccak256`，把哈希当证明 ID。

- `attest(data)` — 计算 `keccak256(data)` 作为 attestationId；首次存证记录时间戳到 `attestedAt`；发 `Attested` 事件。
- 特点：积分记录和哈希都上链，游戏结束后不可篡改、公开可验。
- 用法：先部署本合约，把地址传给 `ScoreAttester`。

---

### 5. CreditcoinAttestation.sol（真实跨链存证，待核对）

**作用**：对接 Creditcoin Attestcoin Protocol（ASC + Block Prover Precompile），用于「链上交互」类任务——验证用户在其它链上真的完成某笔交易后才给分。

**关键点**：
- `VERIFIER` = `0x0FD2`（Block Prover Precompile 地址）。
- `processedQueries` 映射做**防重放**：每个跨链证明只能消费一次。
- `INativeQueryVerifier` 接口：包含 `MerkleProofEntry { hash, isLeft }` 结构和 `verify(...)` 方法，验证源链交易确实被包含在已确认区块中。
- `attest(data)` — 同 SelfAttestation，用 keccak 当证明 ID。
- `verifyOnchainTask(...)` — 先算 queryId、查重放，再调 precompile 的 `verify` 验证；成功后标记已消费并返回 queryId。

**⚠️ 待确认项**（README 已列）：
- `INativeQueryVerifier` 的精确 ABI（`MerkleProofEntry` 字段、`verify` 签名）尚未核对。
- precompile 不校验交易是否成功，需用 EvmV1Decoder 解析 `receipt.status`，MVP 暂未实现。
- 参考仓库：https://github.com/gluwa/usc-testnet-bridge-examples

---

## 四、几个关键设计思想

1. **积分单一权威源**：分数只存在 `ScoreAttester`，`FactionRegistry` 不存分。
2. **存证解耦**：核心合约只依赖 `IAttestation` 接口，换存证实现无需改核心逻辑。
3. **防女巫**：`playerTeam` 映射保证一个地址只能加入一个团队。
4. **状态机**：`NOT_STARTED → ACTIVE → FINISHED`，比赛结束后积分锁定、无法再加分。
5. **接口最小化**：跨合约读取都用接口，用 bool 代替 enum 避免类型不一致。

---

## 五、完整测试流程（Remix）

1. Owner 调 `FR.createFaction("阵营A")`、`FR.createFaction("阵营B")`
2. 两个玩家分别 `FR.createTeam("队1", 1)`、`FR.createTeam("队2", 2)`
3. Owner 调 `FR.startGame()`
4. 后端调 `SA.addScore(1, 任务ID, 100)` 给队 1 加 100 分
5. Owner 调 `FR.endGame()`
6. Owner 往 `PP` 充值：调 `depositPrize()` 填 `value`（如 1 ether）
7. Owner 调 `PP.settlePrizes()`
8. 队 leader 用对应地址调 `PP.claimReward(teamId)` 领奖
