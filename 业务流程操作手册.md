# Attest Arena — 业务流程操作手册（Remix 本地）

> 前提：你已经部署完 4 个合约，并且记录了地址：
> - `AT` = SelfAttestation 地址
> - `FR` = FactionRegistry 地址
> - `SA` = ScoreAttester 地址
> - `PP` = PrizePool 地址

---

## 一、账号分工（在 Remix 的 Account 下拉框里切换）

| 账号 | 角色 | 要做什么 |
|---|---|---|
| **Account 1** | Owner（部署者） | 建阵营、授权后端、开局、结束、充值、结算 |
| **Account 2** | Backend（后端） | 给团队加分 |
| **Account 3** | 玩家 A / 队 1 的 leader | 建队 1、最后领奖 |
| **Account 4** | 玩家 B / 队 2 的 leader | 建队 2 |

> 判断团队/阵营的 ID：第一个创建的阵营是 **1**、第二个是 **2**；第一个创建的团队是 **1**、第二个是 **2**。

---

## 二、流程总览（严格按这个顺序）

```
1. Owner 建两个阵营（faction 1、2）
2. Owner 授权后端（Account 2）
3. 玩家 A 建队 1（进阵营 1）   → 团队 1
4. 玩家 B 建队 2（进阵营 2）   → 团队 2
5. Owner 开局（startGame）
6. Backend 给队 1 加 100 分
7. Owner 结束（endGame）
8. Owner 充值奖池（1 ether）
9. Owner 结算（settlePrizes）
10. 队 1 leader（Account 3）领奖
```

---

## 三、详细步骤（按账号分组）

### 【Account 1 = Owner】建阵营 + 授权后端

在 `Deployed Contracts` 里展开 **FactionRegistry**，找到它的函数按钮：

1. 点 `createFaction`，输入 `"阵营A"`，点 transact → 得到 faction 1
2. 再点 `createFaction`，输入 `"阵营B"`，点 transact → 得到 faction 2

展开 **ScoreAttester**：

3. 点 `setBackend`，两个输入框填：
   - `backend`：Account 2 的地址（切到 Account 2 复制，或用下拉框里显示的值）
   - `authorized`：`true`
   - 点 transact

> 怎么拿到 Account 2 的地址：把 Account 下拉框切到第 2 个账号，点旁边的「复制」图标，再切回 Account 1。

### 【Account 3 = 玩家 A】建队 1

4. 把 Account 下拉框切到 **Account 3**。
5. 展开 **FactionRegistry**，点 `createTeam`，输入：
   - `name`：`"队1"`
   - `factionId`：`1`
   - 点 transact → 得到团队 1，leader 是 Account 3

### 【Account 4 = 玩家 B】建队 2

6. 把 Account 下拉框切到 **Account 4**。
7. 点 `createTeam`，输入：
   - `name`：`"队2"`
   - `factionId`：`2`
   - 点 transact → 得到团队 2，leader 是 Account 4

### 【Account 1 = Owner】开局

8. 切回 **Account 1**。
9. 展开 **FactionRegistry**，点 `startGame`，点 transact。

### 【Account 2 = Backend】加分

10. 把 Account 下拉框切到 **Account 2**。
11. 展开 **ScoreAttester**，点 `addScore`，输入：
    - `teamId`：`1`
    - `taskId`：`1001`（随便一个任务编号）
    - `scoreChange`：`100`
    - 点 transact → 队 1 加 100 分

> 可选：再调一次 `addScore(2, 1002, 0)` 给队 2 记 0 分也行，但不加也不会出错。最终阵营 1 分数更高，所以阵营 1 获胜。

### 【Account 1 = Owner】结束比赛

12. 切回 **Account 1**。
13. 展开 **FactionRegistry**，点 `endGame`，点 transact。

### 【Account 1 = Owner】充值奖池

14. 展开 **PrizePool**，找到 `depositPrize`。
15. **重点**：调用前，先在面板**顶部**（Account 下拉框下面）找到 **VALUE** 输入框，填 `1`，单位选 `ether`。
16. 再点 `depositPrize`（按钮旁会显示 value=1 ether），点 transact。

### 【Account 1 = Owner】结算

17. 展开 **PrizePool**，点 `settlePrizes`，点 transact。

### 【Account 3 = 队 1 leader】领奖

18. 把 Account 下拉框切到 **Account 3**（队 1 的 leader）。
19. 展开 **PrizePool**，点 `claimReward`，输入 `teamId`：`1`，点 transact。
20. 领奖成功后，Account 3 的余额会增加约 1 ether。

---

## 四、如何验证结果（只读查询，蓝色按钮，不用切账号）

这些 `view` 函数点一下就能看，不会发交易：

| 在哪个合约 | 函数 | 填什么 | 应该看到 |
|---|---|---|---|
| ScoreAttester | `getTeamScore` | `1` | `100` |
| ScoreAttester | `getFactionScore` | `1` | `100` |
| ScoreAttester | `getTeamRecordCount` | `1` | `1`（有 1 条加分记录） |
| FactionRegistry | `isFinished` | — | `true` |
| PrizePool | `settled` | — | `true` |
| PrizePool | `rewards` | Account 3 的地址 | 结算后的金额（领奖前） |
| PrizePool | `totalPrizePool` | — | `1000000000000000000`（=1 ether） |

---

## 五、常见报错排查

| 报错关键字 | 原因 | 解决 |
|---|---|---|
| `SA: not backend` | 用错了账号调 `addScore`，或没授权后端 | 确认用 Account 2，且先跑过 `setBackend` |
| `SA: game not active` | 没 `startGame` 就加分，或已经 `endGame` | 按顺序：先 startGame 再加分 |
| `FR: not owner` | 用了非部署账号调 owner 函数 | 切回 Account 1 |
| `PP: game not finished` | 没 `endGame` 就结算 | 先调 `endGame` |
| `PP: empty pool` | 没充值就结算，或 VALUE 没填 | 检查 `depositPrize` 时 VALUE 是否填了 |
| `PP: only leader` | 领奖账号不是该队 leader | 用 Account 3 领队 1 的奖 |
| `FR: already in a team` | 同一地址重复建队/加队 | 换新账号，一个地址只能一队 |
| `FR: bad faction` / `FR: bad team` | 阵营/团队 ID 填错 | 阵营 ID 是 1、2；团队 ID 是 1、2 |

---

## 六、整个流程跑通后你会看到

1. 队 1 得分 100，阵营 1 得分 100（阵营 2 为 0）。
2. 结算时阵营 1 获胜，独得全部 1 ether。
3. 因为阵营 1 里只有队 1 一个团队，空缺的 2、3 名份额和剩余 10% 都并入第 1 名，所以队 1 拿满 100%。
4. Account 3（队 1 leader）调 `claimReward(1)` 后收到 1 ether。
